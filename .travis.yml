language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-front-end-module' ]; then cd .. && eval "mv $currentfolder generator-front-end-module" && cd generator-front-end-module; fi

